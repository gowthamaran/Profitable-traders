[pytest]
asyncio_mode = auto
testpaths = tests
# The package is imported from the repo root, so a bare `pytest` works the
# same as `python -m pytest`.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
